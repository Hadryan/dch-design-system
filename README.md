# Digital Concert Hall Design System Documentation

# Naming Conventions

* all files and folders lowercase
* no spaces, use `_` (dash)
* no underscores `_`

## Development To-Do

* Tabs
  * Improve current implementation (visually looks broken)
  * Try to implement a better one without javascript reliance
* Content (management)
	* Move images to proper folders & re-link
	* Can we have dynamic urls? link using handles rather than url
	* Classify the atomic elements using tags/taxonomy
  * Can we embed Zeplin images on html?
* Content
	* Backend & Applications
	  * Double-check all links
    * Add proper "states" to items
    	* default
    	* hover/focus
    	* press
    	* selected
    	* disabled